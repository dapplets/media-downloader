{
    "type": "object",
    "required": [
        "network",
        "useCustomNode",
        "searchEngineUrl"
    ],
    "properties": {
        "network": {
            "type": "string",
            "title": "Target network",
            "enum": [
                "goerli",
                "xdai"
            ]
        },
        "useCustomNode": {
            "type": "boolean",
            "title": "Use custom node settings"
        },
        "swarmGatewayUrl": {
            "type": "string",
            "title": "Custom Swarm gateway URL"
        },
        "operatorAddress": {
            "type": "string",
            "title": "Custom operator address"
        },
        "searchEngineUrl": {
            "type": "string",
            "title": "Search Engine URL"
        }
    }
}